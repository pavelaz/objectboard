,<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="content-type" content="text/html;charset=UTF-8">
        <title>First JSP</title>
    </head>
    <body>
        <p><c:out value="Saludos Alex, bienvenido a tu clase de JSTL"/></p>
        <p><c:out value="${ variable }" default="Valor por defecto" escapeXml="false"/></p>
        <p><c:out value="${ variable }" escapeXml="false">Valor por defecto sintaxis diferente</c:out></p><!--escapeXml en false, cancela el recibimiento de codigo alternativo por un input-->

        <c:set var="variable" value="Mateo" scope="page"/> <!--scope="application/page/request/session" duracion valida de la variable -->
        <p><c:out value="${variable}"/></p>
        <c:set var="variable" scope="page">Alex</c:set><!--Otra manera de inicializar la variable-->
        <p><c:out value="${variable}"/></p>

        <!--Trabajando con Beans Objects Java, tambien podemos modificarlos a traves de la siguiente instruccion-->
        <c:set target="${ autor }" property="nombre" value="Mathiu"/>
        <p><c:out value="${ autor.nombre }"> No tengo valor aun!</c:out></p>

        <!--Si queremos suprimir la variable de la memoria-->
        <c:remove var="variable" scope="page"/>

        <!--Manejando condiciones-->

        <c:if test="${ 50 > 10 }" var="variable" scope="page"><!--variable trae resultado true o false dependiendo la condicion -->
                    Es cierto!
                </c:if> <!--Hay un defecto en esta instruccion que no maneja else-->

           <c:choose>
                    <c:when test="${ variable }">Du texte</c:when>
                    <c:when test="${ otravariable }">Du texte</c:when>
                    <c:when test="${ nuevamenteOtravariable }">Du texte</c:when>
                    <c:otherwise>Ninguna de las anteriores</c:otherwise>

           </c:choose>

       <!--Manejando bucles-->

        <c:forEach var ="i" begin="0" end="10" step="1"> <!--tenemos que tener cuenta que siempre avancara uno mas del numero definino en end-->
            <p> Un mensaje N. <c:out value ="${i}"/> !</p>
        </c:forEach>
        </br>
        <c:forEach var ="i" begin="0" end="10" step="2"> <!--de 2 en 2-->
            <p> Un mensaje N. <c:out value ="${i}"/> !</p>
        </c:forEach>

        <!--Interando en un arreglo de String definido desde la Servlet
        BonjourServlet-->
        Definimos lo siguiente dentro del metodo doGet de la Servlet

        String[] titulos ={ "Nuevo incendio","Una isla fue descubierta", "Cascada de dolares" };
        req.setAttribute("titulos",titulos);


        <c:forEach items ="${ titulos }" var ="titulo"> <!--tenemos que tener cuenta que siempre avanzara uno mas del numero definino en end-->
            <p><c:out value ="${ titulo }"/> !</p>
        </c:forEach>
        </br>
        <c:forEach items ="${ titulos }" var ="titulo" begin="0" end ="1"> <!--solo dos primeros-->
            <p><c:out value ="${ titulo }"/> !</p>
        </c:forEach>
        </br>
        <c:forEach items ="${ titulos }" var ="titulo" varStatus="status"> <!--tenemos que tener cuenta que siempre avanzara uno mas del numero definino en end-->
            <p>N.<c:out value="${status.count}"/> : <c:out value ="${ titulo }"/> !</p>
        </c:forEach>

        <!--For especial para cadena de caracteres-->
        <c:forTokens var="pedazo" items="un elemento/otro elemento/ultimo elemento" delims="/">
            <p>${pedazo}</p>
        </c:forTokens>
        <c:forTokens var="pedazo" items="un elemento/otro elemento/ultimo elemento" delims="/ "> <!--interesante delims-->
            <p>${pedazo}</p>
        </c:forTokens>

        <!--Para mas informacion JSTL consultar: https://docs.oracle.com/javaee/5/tutorial/doc/bnahq.html#bnaij-->
        https://youtu.be/or1T5fnOSIE
        Funsion de conversion binaria.
        <fmt:parseNumber var="j" integerOnly="true" type="number" value="${ i }"/>
        <jstl:url value="index.jsp" var=""theURL">
            <jstl:param name="passedParam" value="passed value"/>
        </jstl:url>
        <jstl:import url="${theURL}"/>

        <jstl:remove var="i"/>

    </body>
</html>

<jstl:set var="emptyTable" value="true" scope="page"/>
                                                    <jstl:set var="valueCell" value="null" scope="page"/>
                                                    <jstl:if test= "${ emptyTable != true }" >
                                                        <jstl:forEach varStatus="status" var="setBussinessTypeDto"  items="${ rq_BussinessTypeDto.setBussinessTypeDtoSet() }" end="${ emptyTable = true }">
                                                            <jstl:set target="${ setBussinessTypeDto }" property="${ bussinessTypeDto.btCodeType }" value=""/>
                                                            <jstl:set target="${ setBussinessTypeDto }" property="${ bussinessTypeDto.btDescription }" value=""/>
                                                            <jstl:set target="${ setBussinessTypeDto }" property="${ bussinessTypeDto.btNote }" value=""/>
                                                        </jstl:forEach>--%>
                                                    </jstl:if>