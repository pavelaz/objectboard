private @Context HttpServletRequest request;
    HttpSession objSesion = request.getSession();

    final String company_name = (String) objSesion.getAttribute("companyName");
    public String company_number = (String) objSesion.getAttribute("companyNumber");
    final String user_email = (String) objSesion.getAttribute("userEmail");
    final String user_name = (String) objSesion.getAttribute("userName");
    CommentsController controller = null;

    /*@GET //regresa una lista de libros
    @Produces(MediaType.APPLICATION_JSON)
    public CommentsGetDto liste(@Context HttpServletRequest request, @Context HttpServletResponse response) throws ServletException, IOException {
        controller = new CommentsController();
        CommentsGetDto commentsGetDto = controller.getCommentsList();
        request.setAttribute("rq_CommentsDto", commentsGetDto);
        request.setAttribute("rq_companyName", company_name);
        request.setAttribute("rq_companyNumber", Integer.parseInt(company_number));
        request.setAttribute("rq_userName", user_name);

        System.out.println("BussinessUnitEntityasterUserServlet metodo 'GET', muestra informacion de formulario");
        request.getRequestDispatcher("WEB-INF/pages/jsp/customers/comments.jsp").forward(request, response);

        return commentsGetDto;
    }*/

    static {
        HttpServletRequest request = null;
        HttpSession objSesion = request.getSession();

        final String company_name = (String) objSesion.getAttribute("companyName");
        String company_number = (String) objSesion.getAttribute("companyNumber");
        final String user_email = (String) objSesion.getAttribute("userEmail");
        final String user_name = (String) objSesion.getAttribute("userName");
        CommentsController controller = null;
        }