package com.psg.objectboard.model.repository;

import com.psg.objectboard.model.EntityManagerHolder;

import javax.persistence.EntityManager;

public class DischargeRepositoryImpl {

    public DischargeEntity getById(Long id){
        EntityManager entityManager =new EntityManagerHolder().getCurrentEntityManager();
        DischargeEntity dischargeEntity= entityManager.find(DischargeEntity.class, id);
        System.out.println("DischargeEntityImpl Read");
        return dischargeEntity;
    }
}
