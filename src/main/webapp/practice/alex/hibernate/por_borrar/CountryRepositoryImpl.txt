package com.psg.objectboard.model.repository;

import com.psg.objectboard.model.EntityManagerHolder;

import javax.persistence.EntityManager;

public class CountryRepositoryImpl {

    public CountryEntity getById(Long id){
        EntityManager entityManager =new EntityManagerHolder().getCurrentEntityManager();
        CountryEntity countryEntity= entityManager.find(CountryEntity.class, id);
        System.out.println("CountryEntityImpl Read");
        return countryEntity;
    }
}
