package com.psg.objectboard.model.repository;

import com.psg.objectboard.model.EntityManagerHolder;

import javax.persistence.EntityManager;

public class StatesRepositoryImpl {

    public StatesEntity getById(Long id){
        EntityManager entityManager =new EntityManagerHolder().getCurrentEntityManager();
        StatesEntity statesEntity= entityManager.find(StatesEntity.class, id);
        System.out.println("StatesEntityImpl Read");
        return statesEntity;
    }
}
