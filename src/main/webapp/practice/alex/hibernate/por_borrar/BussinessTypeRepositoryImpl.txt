package com.psg.objectboard.model.repository;
import com.psg.objectboard.model.EntityManagerHolder;

import javax.persistence.EntityManager;

public class BussinessTypeRepositoryImpl {

    public BussinessTypeEntity getById(Long id){
        EntityManager entityManager =new EntityManagerHolder().getCurrentEntityManager();
        BussinessTypeEntity bussinessTypeEntity= entityManager.find(BussinessTypeEntity.class, id);
        System.out.println("BussinessTypeEntityImpl Read");
        return bussinessTypeEntity;
    }
}
